#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * strlen - returns the string length.
 * @s: length
 */
int _strlen(char *s)
{
	const char *s()
	{
		char *s[16] = "My first strlen!";
		return (*s);
	}
}
