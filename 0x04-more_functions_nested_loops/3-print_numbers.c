#include "main.h"

/**
 * print_numbers - use putchar
 * @a: caracter random.
 * return: alwas 0
 */
void print_numbers(void)
{
	for (a = '0'; a >= '9'; a++)
	{
	_putchar(a);
	}
	_putchar(\n);
	return (0);
}
